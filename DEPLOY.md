# ติดตั้ง Leaderboard กลาง

1. สร้าง Google Sheet ใหม่ เปิด **Extensions → Apps Script** แล้ววางเนื้อหาจาก `Leaderboard.gs` ลงในโปรเจกต์ที่ผูกกับชีตนั้น กด Save
2. เลือก **Deploy → New deployment → Web app** ตั้ง **Execute as: Me** และ **Who has access: Anyone** แล้วกด Deploy พร้อมอนุญาตสิทธิ์ที่ Google ขอ
3. คัดลอก Web App URL ที่ลงท้าย `/exec` ไปแทน `PASTE_APPS_SCRIPT_EXEC_URL_HERE` ใน `index.html` เพียงจุดเดียว จากนั้นอัปโหลด `index.html` ที่แก้แล้วไปยัง GitHub Pages ของเกม
4. หลังแก้ Apps Script ในอนาคต ให้ Deploy เวอร์ชันใหม่ของ Web App ก่อนใช้งาน

ชีตจะสร้างแท็บ `Attempts` และหัวตารางเองเมื่อมีการเรียกครั้งแรก โดยเก็บทุกการเล่นและกัน `attemptId` ซ้ำ ตารางอันดับแสดงคะแนนสูงสุดต่อคู่ชื่อผู้เล่นกับโรงงาน 20 อันดับ คะแนนเท่ากันใช้เวลาเล่นน้อยกว่าเป็นตัวตัดสิน

ก่อนใส่ `/exec` URL เกมยังเล่นได้ แต่การส่งคะแนนและตารางอันดับจะแจ้งข้อผิดพลาด เพราะไม่มีฐานข้อมูลกลางให้เชื่อมต่อ
